# AGENTS.md

This file provides guidance to OpenAI Codex when working with this repository.

## Context

**Read `llms.txt` first** for the repo’s source-of-truth context and documentation links.

## Quality bar

- Run `yarn lint && yarn build && yarn test` before considering work complete.
- Do not skip tests to get a PR green.
