# canton-node-sdk

## Step 0: Read the repository documentation

Start with [`README.md`](README.md) and the focused guides under [`docs/`](docs/README.md). Use
[`src/index.ts`](src/index.ts), generated-client source, runnable examples, and tests as the source
of truth for exact behavior. Before handing off a change, run the relevant checks listed in
`README.md` and [`package.json`](package.json).
